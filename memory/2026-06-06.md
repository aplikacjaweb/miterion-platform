# 2026-06-06 - GDPR Compliance Updates

## Summary
- **Goal**: Refactor `CookieConsentBanner.tsx` to ensure GDPR/ePrivacy compliance by blocking Google Analytics and Facebook Pixel until explicit user consent.
- **Key Changes**:
  1. Added prior consent logic for Google Analytics (`gtag.js`) and Facebook Pixel (`fbevents.js`).
  2. Implemented persistent consent using `localStorage` (`cookie_consent: 'granted'`).
  3. Added a "Decline" button to allow users to reject tracking.
  4. Updated banner text to clarify cookie usage and link to the privacy policy.
- **Verification**:
  - Use Chrome DevTools (Network tab) to confirm scripts only load after consent.
  - Check `localStorage` for `cookie_consent` state.
  - Ensure the banner does not reappear after consent is given.
- **Next Steps**:
  - Replace placeholder IDs (`GA_MEASUREMENT_ID`, `YOUR_FACEBOOK_PIXEL_ID`) with actual tracking IDs.
  - Test in staging before production deployment.
  - Consider granular consent controls (e.g., separate toggles for analytics/marketing).

## Files Modified
- `src/components/CookieConsentBanner.tsx`: Refactored to enforce prior consent and add decline functionality.
- `src/app/privacy/page.tsx`: Verified alignment with privacy policy.

## Commands Executed
- `git add src/components/CookieConsentBanner.tsx`
- `git commit -m "Refactor CookieConsentBanner for GDPR compliance: Add prior consent logic, Decline button, and persistent localStorage"`
- `git status` (multiple checks to confirm changes)

## Pending Actions
- Push changes to the remote repository (`git push`).
- Verify deployment in the live environment.
- Monitor for further legal/compliance updates.

## Notes
- No other files in the codebase load Google Analytics or Facebook Pixel outside the consent banner.
- The privacy policy (`/privacy-policy`) reflects the use of these scripts and the consent mechanism.

## Privacy Policy Updates
- **Goal**: Update the privacy policy to include new sections for "Profiling," update the "International Data Transfers" section, and add a "Governing Law" section.
- **Key Changes**:
  1. Added Section 3a: "Profiling" to ensure GDPR compliance.
  2. Updated Section 7: "International Data Transfers" to include EU-U.S. Data Privacy Framework (DPF) and Standard Contractual Clauses (SCCs).
  3. Added Section 12: "Governing Law" to specify Polish jurisdiction.
  4. Fixed section numbering to maintain logical flow.
- **Files Modified**:
  - `src/app/privacy/page.tsx`: Updated to include new sections and ensure compliance with GDPR and Polish data protection laws.
- **Commands Executed**:
  - `git add src/app/privacy/page.tsx`
  - `git commit -m "Update privacy policy: fix numbering, add Profiling (3a), add Analytics (5), update International Transfers (7), and add Governing Law (12)"`
  - `git push origin main`
- **Status**: Successfully pushed to the remote repository.
